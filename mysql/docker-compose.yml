version: '3'
services:
  mysql:
    image: mysql
    environment:
      - MYSQL_ROOT_PASSWORD=123123
    container_name: mysql
    network_mode: host
    volumes:
      - ~/volumes/mysql/:/var/lib/mysql/

